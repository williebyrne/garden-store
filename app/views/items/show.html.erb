<p id="notice"><%= notice %></p>

 <div id="itemsContainer">
    <h1><%= @item.title %></h1>
    <%= image_tag @item.image_url, :class => 'welcomIm'  %>
    <h2><%= @item.description %></h1>
   
    <div class="itemhols">
      <p>Price <%= number_to_currency(@item.price, :unit => "€") %>
        <br>
          Category: <%= @item.category %>
        <br>
        Brand: <%= @item.brand %>
      </p>
       
      <p>
        <a href="/cart/<%= @item.id %>" class="button">Add to Cart</a>
        <%= link_to 'Back to Products', items_path, :class => "button", :role => "button" %>
        
        <% if user_signed_in? %>
        <% if current_user.admin? %>
        <%= link_to 'Edit', edit_item_path(@item), :class => "button", :role => "button" %>
        <% end %>
        <% end %>
       
      </p>
    </div>
  </div>
